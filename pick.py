from repository.maria import chart, corp

# def main():
#     # 거래정지 종목 제거 - 거래 정지 여부 정확히 알수 없지만 아래 조건은 거래 정지 종목으로 보임
#     current_chart = chart.get_month_chart()
#     current_chart = current_chart[current_chart.apply(lambda x: x['cap'] != 0, axis=1)]
#     current_chart = current_chart[current_chart.apply(lambda x: x['vol'] != 0, axis=1)]
#     pass

if __name__ == '__main__':
    # main()
    # chart.update_month_chart(2022, 10)
    print(1.3 ** 25)
